export enum MetricAggregator {
  Count =              "count",
  Sum =                "sum",
  Unique =             "unique",
}

export enum MetricScope {
  Event =              "event",
  Visitor =            "visitor",
}

export enum WinningDirection {
  Increase =           "increasing",
  Decrease =           "decreasing",
}
